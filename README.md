# Psseudocode-to-check-a-number-magic-or-not
BEGIN 
     GET a number
     ASSIGN as num
     IF num % 9 == 0
       DISPLAY num is magic number
     ELSE
       DISPLAY enter a new number
     ENDIF
END
